{
  "name": "doodle-draw-server",
  "version": "0.0.1",
  "description": "Sample socket.io game server",
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.1"
  },
  "scripts": {
    "clean": "rm -rfv ./dist/",
    "build": "tsc --build",
    "start": "node dist/server.js",
    "dev": "concurrently \"tsc --watch\" \"nodemon dist/server.js\""
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.4.2",
    "concurrently": "^8.2.0",
    "nodemon": "^2.0.15",
    "typescript": "^5.1.6"
  }
}
